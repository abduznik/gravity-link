name: Multi-Platform Standalone Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Bundle Client & Scripts
        run: npm run bundle

      - name: Run Typecheck
        run: npm run typecheck

      - name: Create Out Directory
        run: mkdir -p out

      - name: Compile Standalone Binaries
        run: |
          npx pkg . --targets node18-macos-x64,node18-macos-arm64,node18-linux-x64,node18-win-x64 --out-path out/

      - name: Rename Binaries for Clean Release Names
        run: |
          mv out/gravity-link-extension-macos-arm64 out/gravity-link-macos-apple-silicon || mv out/gravity-link-extension-macos out/gravity-link-macos-apple-silicon || true
          mv out/gravity-link-extension-macos-x64 out/gravity-link-macos-intel || true
          mv out/gravity-link-extension-linux-x64 out/gravity-link-linux-x64 || mv out/gravity-link-extension-linux out/gravity-link-linux-x64 || true
          mv out/gravity-link-extension-win-x64.exe out/gravity-link-windows.exe || mv out/gravity-link-extension-win.exe out/gravity-link-windows.exe || true
          ls -la out/

      - name: Upload Binaries to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'latest' }}
          name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'Latest Standalone Build' }}
          files: |
            out/gravity-link-macos-apple-silicon
            out/gravity-link-macos-intel
            out/gravity-link-linux-x64
            out/gravity-link-windows.exe
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
